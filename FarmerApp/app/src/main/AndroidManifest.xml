<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aliasghar.farmerapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RegisterAs">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ForgotPassword" />
        <activity android:name=".FarmerRegistrationForm" />
        <activity android:name=".MillsRepresentativeRegistrationForm" />
        <activity android:name=".HomebuyerRegistrationForm" />
        <activity android:name=".MainActivity" />
        <activity android:name=".WholesalerRegistrationForm" />
        <activity android:name=".PostCrops" />
        <activity android:name=".Dashboard" />
        <activity android:name=".OpenTendors" />
        <activity android:name=".ClosedTendors" />
        <activity android:name=".UserPosts" />
        <activity android:name=".CropsBid" />
        <activity android:name=".ViewBIds" />
        <activity android:name=".ViewUserBids"></activity>
    </application>

</manifest>